# Affix/CA  ·  Environment Configuration
# Copy this to .env and edit as needed

# Port the CA web UI listens on (host side, HTTPS)
CA_PORT=443

# Port for public PKI distribution (CRL, AIA, chain — HTTP)
CA_HTTP_PORT=80
